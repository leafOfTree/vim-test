call system('rm -r /tmp/vim-project-config')

let g:vim_project_config = {
      \'home': '/tmp/vim-project-config/', 
      \'auto_detect': 'no',
      \'session': 1, 
      \'open_entry': 1, 
      \}
